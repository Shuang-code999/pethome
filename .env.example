# 萌宠之家 · 根 .env 模板（docker compose 自动读取根目录 .env）
# 用法：cp .env.example .env 后填真实值。本文件只放 compose 层需要覆盖的变量。
#
# 注意：API 密钥（BAILIAN/ALIPAY/TENCENT/WEATHER/AMAP/UNSPLASH/ALIYUN）仍由
#       backend/.env 提供（compose 的 backend 服务用 env_file 加载），无需写在这里。
#       这里只覆盖「与 docker 拓扑/安全」相关的少量变量。

# JWT 签名密钥：生产务必改成一个 ≥32 字符的强随机串（dev 默认值不安全）
# 可用 openssl rand -base64 48 生成
JWT_SECRET=prod-please-change-jwt-secret-2026-min32chars

# 如需覆盖 MySQL/RabbitMQ 密码，在这里设（同时改 compose 里对应值保持一致）
# DB_PASSWORD=root123
# MQ_USERNAME=pet
# MQ_PASSWORD=pet123
